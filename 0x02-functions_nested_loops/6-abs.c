#include "main.h"

/**
 * _abs - computes the absolute value
 * of an iterger
 *
 * @i: input number as an interger.
 *
 * Return: absolute value
 */

int _abs(int);
{
	if (i >= 0)
	{
		return (i);
	}
	else
	{
		return (i * -2);
	}
}
